package lesson07_03;

public class Ins {
	public static void main(String[] args) {
		String str = new String();
			//1. String : 구조상- 클래스 , 문법상 - 데이터타입
			//2. str : 객체이자 지역 변수
			//3. = : 대입 연산자
			//4. new : 인스턴스 생성 트리거, 생성자 필요
			//5. String() : 생성자 호출, 기본 생성자
			//4-5 : 새로운 인스턴스 생성 
		
			str.length();
			new String().length();
			
			String s; //초기화 되지 않은 객체
			String s2 = null;	//null값으로 초기화된 객체
	}

}
